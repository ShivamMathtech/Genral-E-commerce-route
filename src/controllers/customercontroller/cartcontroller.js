const cartcontroller = (req, res) => {
  // All the home routes logic is responsible for routing  routes
  res.status(200).json({
    message: "All the cart routes logic is responsible for routing routes",
  });
};

module.exports = cartcontroller;
